# 明日开工计划 · 2026-09-17（照做即可）

> 前置：P08 已修好并收口（commit 52dca9b），E1 全片 72.48s 已验收通过。
> 本文件是"开工即执行"清单，按顺序做，做完一项勾一项。

## 步骤 1：P01-P14 全片回扫（第一优先，预算 30-60 分钟）

P08 踩的"多手/双人"坑，其他 13 镜大概率也有。重点怀疑：

| 镜 | 风险点 | 抽检重点 |
|---|---|---|
| P04 怪梦段 | 床边人影+她两只手 | 人手不重、人影虚焦一闪不出脸 |
| P03 视频通话 | 她单手举手机 | 手机背面朝观众不露屏、杨永川只走画外 |
| P07/P10/P12 挂坠特写 | 特写手部 | 手不重、挂坠与定妆一致 |
| P13 东北小站 | 剪影 | 不露脸、雪氛围对 |

抽检方法：每镜 ffmpeg 抽 0/2/4s 三帧，对照 [E1_prompt_规格基准.md](E1_prompt_规格基准.md) 两条纪律：
- **手部纪律**：single person / exactly two hands / no duplicated person
- **物件一致性**：泡面桶=P02 同款橙条纹、睡衣浅蓝碎花、笔记本合盖朝镜头、手机不露屏

跑偏镜修：首帧 i2i 重出 → keyframe 出片 → 抽检 → 覆盖 PXX_test.mp4 → 重拼全片。
预算：预计跑偏 3-5 镜，每镜端到端 3-5 分钟（含 503/429 重试）。

## 步骤 2：E2 返乡铺开（回扫无雷后开）

1. 写 E2 逐镜 prompt（照 E1 14 镜规格：钩子 3s / 矛盾搭建 / 尾钩；挂坠温度线落点 E2"无故微热"=报马来报）
   - 关键信物节奏：E2 只出现挂坠，**香炉是 E4 才给**，别提前
   - 周野首登场（开车接站、扶着岁数大的刘桂兰，话少）
2. 首帧 i2i 出图：宋栀_v2 + 玉坠定妆照锁脸，周野/刘桂兰定妆照已入库直接引用
3. 出片 14 镜 → 每镜抽 0/2/4s 三帧验收 → 拼接 E2 全片

## 步骤 3：配音/字幕/配乐层（E2 验收后）

- 报马画外音（御姐冷稳音）逐镜对位——台词不进视频 prompt 的规矩保住
- 音效层：钱债线"转账叮"放大（E1 P03 同款 E11 再现）；挂坠温度线（热/凉/微热）
- 字幕压进画面，逐镜对位

## 步骤 4：E3-E10 铺开节奏

E2 验收通过后开 E3。每集开铺前：首帧 i2i 验收 → 批量出片 → 抽帧验收 → 拼接。

## 铁律备忘（避免再犯）

1. **翻车先定位环节**：抽视频帧看是首帧带病还是插帧跑偏，别一翻车就只改首帧
2. **人手纪律写 prompt**：i2i 和 keyframe 都写「single person, exactly two hands, no duplicated person, no second phone」
3. **429 限速**：批量出片逐条提交、间隔 60-90s，不撞窗口；免费版恢复周期 60-120s
4. **503 video_queue_full**：脚本已内置自动重试（30s×N），跑偏镜重出不急
5. **首帧中间版保留**：v2/v3 留在 frames/ 供回滚对照，定妆版才覆盖 PXX_first.png
6. **香炉节奏**：E2 不出现香炉（E4 才给），挂坠是 E1 起一直佩戴的报马暗号

## 工具速查

```bash
# 出首帧 i2i（锁脸+服装）
python scripts/gen_image.py --prompt "..." --ref "05_输出/定妆照/宋栀_v2.png" --ref "05_输出/E1/frames/P02_first.png" --size 736x1312 --out "05_输出/E2/frames/P01_first.png"

# 出片 keyframe
python scripts/gen_video.py --prompt "..." --img "05_输出/E2/frames/P01_first.png" --out "05_输出/E2/P01_test.mp4" --timeout 900

# 抽检抽帧
ffmpeg -i P0X_test.mp4 -ss 0 -vframes 1 f0.png -ss 2 -vframes 1 f2.png -ss 4 -vframes 1 f4.png

# 拼接
ffmpeg -f concat -safe 0 -i concat_list.txt -c copy E2_全片拼接_试片版.mp4
```

## 完成判定
- [ ] P01-P14 回扫无"多手/双人"，E1 全片验收通过
- [ ] E2 逐镜 prompt 写完并入库
- [ ] E2 14 镜出片 + 抽帧验收 + 拼接
- [ ] 配音/字幕/配乐至少挂进 E1 全片
- [ ] 全部 commit + push
